<div class="results-block">
    <%= render 'layouts/messages'%>
</div>

<% if @friends %>
    <div class="card card-header results-block">
    <table class="col-md-12">
        <tbody>
            <% @friends.each do |friend|%>
                <strong>Name:</strong><%= friend.full_name%>
                <strong>Email:</strong><%= friend.email%>
                <strong>Profile:</strong><%= link_to "View Profile", user_path(friend), class: "btn btn-sm btn-success m-3" %>
                <% if current_user.not_friends_with?(friend.id)%>
                <%= link_to 'Follow Friend?', friendships_path(friend: friend), class: "btn btn-sm btn-success", method: :post %>
                <%else%>
                    <span class="badge badge-secondary"> You Are Friend</span>
                <%end%>
            <%end%>
        </tbody>
    </table>
    </div>
<%end%>